program: main.py
method: grid
project: gamess-gnn-sweep

metric:
  name: avg_val_mse
  goal: minimize

parameters:
  # Validation configuration
  validation_method:
    values: ['random_split', 'per_element', 'per_molecule']
  
  validation_subset:
    value: null  # null = all elements/molecules, or specify list like ['H', 'C'] or ['b2', 'h2o']
  
  val_split_ratio:
    value: 0.2  # Only used for random_split method
  
  random_seed:
    value: 42  # For reproducibility
  
  # Model and training parameters
  normalization_enabled:
    values: [true, false]
  
  normalization_global:
    values: [true, false]

  pooling_method:
    values: ['sum', 'mean', 'attention']
  
  loss_balancing_strategy:
    values: ['static', 'gradnorm', 'first_epoch']

  occupation_weight:
    values: [1, 2]

  keibo_weight:
    value: 1

  energy_weight:
    values: [0.01, 0.5]

  hidden_dim:
    values: [32, 64]

  orbital_embedding_dim:
    values: [32, 64, 128]

  num_layers:
    values: [3, 4]

  epochs:
    values: [2]

command:
  - ${env}
  - python
  - ${program}
  - --sweep