apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mcscfgnn-3
  namespace: gai-lina-group
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: rook-cephfs-central
  resources:
    requests:
      storage: 100Gi